spec:
  # Everything under here follows JSON schema (https://json-schema.org/), written as YAML for readability
  type: object
  properties:
    format_version:
      type: string
      minLength: 1
      pattern: "^[0-9]+\.[0-9]+\.[0-9]+$"
    name:
      type: string
      minLength: 1
    title:
      type: string
      minLength: 1
    description:
      type: string
      minLength: 1
    version:
      type: string
      minLength: 1
      pattern: "^[0-9]+\.[0-9]+\.[0-9]+$"
  required:
  - format_version

  ##
  ## TODO: WIP!
  ##