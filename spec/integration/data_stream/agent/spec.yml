spec:
  additionalContents: false
  contents:
  - description: Folder containing agent definitions
    type: folder
    name: stream
    required: true
    additionalContents: false
    contents:
    - description: Folder containing agent stream definitions
      type: file
      sizeLimit: 2MB
      pattern: '^.+.yml.hbs$'
      required: true
