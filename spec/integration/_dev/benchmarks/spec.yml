spec:
  additionalContents: false
  developmentFolder: true
  contents:
    - description: Folder containing end-to-end benchmarks scenarios.
      type: folder
      name: e2e
      required: false
      additionalContents: true
      contents:
        - description: Folder containing deployable services for benchmarks.
          type: folder
          name: deploy
          additionalContents: true
          required: false
          contents:
            - description: Folder containing docker compose files for benchmarks.
              type: folder
              name: docker
              additionalContents: true
              required: false
              contents:
                - description: Docker Compose configuration file for deploying benchmarks additional services.
                  type: file
                  name: "docker-compose.yml"
                  contentMediaType: "application/x-yaml"
                  required: true
        - description: A benchmark scenario.
          type: file
          pattern: '^.+\.yml$'
          contentMediaType: "application/x-yaml; require-document-dashes=true"
          required: false
          $ref: './scenario.spec.yml'