format_version: 3.6.0
name: bad_input_both_template_path
title: "Bad Input Both Template Path"
version: 0.0.1
source:
  license: "Apache-2.0"
description: "Test package with both template_path and template_paths (invalid)."
type: input
categories:
  - custom
conditions:
  kibana:
    version: "^8.0.0"
  elastic:
    subscription: "basic"
screenshots:
  - src: /img/sample-screenshot.png
    title: Sample screenshot
    size: 600x600
    type: image/png
icons:
  - src: /img/sample-logo.svg
    title: Sample logo
    size: 32x32
    type: image/svg+xml
policy_templates:
  - name: sample
    type: logs
    title: Sample logs
    description: Collect sample logs
    input: logfile
    template_path: input.yml.hbs
    template_paths:
      - input1.yml.hbs
      - input2.yml.hbs
    vars:
      - name: paths
        type: text
        title: Paths
        multi: true
        default:
          - /var/log/*.log
elasticsearch:
  index_template:
    mappings:
      subobjects: false
owner:
  github: elastic/foobar
  type: community
