format_version: 3.7.0
name: good_columnar_override
title: Good package with field-level columnar overrides
description: Tests that a field can re-enable doc values for columnar index modes with the columnar override block.
version: 1.0.0-beta1
type: integration
source:
  license: "Apache-2.0"
conditions:
  kibana:
    version: "^9.5.0"
  elastic:
    subscription: basic
policy_templates:
  - name: logs
    title: Logs
    description: Collect logs
    inputs:
      - type: logfile
        title: Collect logs from files
        description: Collecting logs from log files
owner:
  github: elastic/obs-infraobs-integrations
  type: elastic
