format_version: 3.7.0
name: good_columnar_base_mode
title: Good package with columnar (base) index mode
description: Tests that columnar (base mode, without logsdb profile) is a valid index_mode and that clean fields pass validation.
version: 1.0.0-beta1
type: integration
source:
  license: "Apache-2.0"
conditions:
  kibana:
    version: "^9.5.0"
  elastic:
    subscription: basic
policy_templates:
  - name: logs
    title: Logs
    description: Collect logs
    inputs:
      - type: logfile
        title: Collect logs from files
        description: Collecting logs from log files
owner:
  github: elastic/obs-infraobs-integrations
  type: elastic
