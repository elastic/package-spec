format_version: 3.6.0
name: bad_built_fs_artifacts
title: Bad Built Package - Source-only FS Artifacts Present
description: Built-package fixture containing a _dev folder and a .link file - both source-only artifacts that must be rejected in build mode (issue #549).
version: 0.0.1
type: integration
source:
  license: "Apache-2.0"
conditions:
  kibana:
    version: '^8.0.0 || ^9.0.0'
policy_templates:
  - name: events
    title: Events logs
    description: Collect events data
    inputs:
      - type: logfile
        title: Collect events logs
        description: Collecting events log data
        multi: false
owner:
  github: elastic/foobar
  type: community
