# Good input package with dynamic_signal_types and type field

This is a test package demonstrating that the type field is optional but still valid when dynamic_signal_types is enabled with otelcol input.
