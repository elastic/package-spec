format_version: 3.7.0
name: bad_columnar_normalizer
title: Bad package with keyword normalizer (columnar mode)
description: Tests that a keyword field with a normalizer is a hard error in columnar index mode.
version: 1.0.0-beta1
type: integration
source:
  license: "Apache-2.0"
conditions:
  kibana:
    version: "^9.5.0"
  elastic:
    subscription: basic
policy_templates:
  - name: logs
    title: Logs
    description: Tests that a keyword field with a normalizer is a hard error in columnar index mode.
    inputs:
      - type: logfile
        title: Collect logs from files
        description: Tests that a keyword field with a normalizer is a hard error in columnar index mode.
owner:
  github: elastic/obs-infraobs-integrations
  type: elastic
