# Shared dev resources

Placeholder for content package _dev/shared. Used to validate that content-only packages allow the _dev directory. This folder can hold dashboard YML sources and other dev-only files.
