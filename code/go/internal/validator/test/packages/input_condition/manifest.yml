format_version: 1.0.0
name: input_condition
title: Input Condition Test Package
description: Package to test conditions under input definitions
release: experimental
version: 1.0.0
owner:
  github: elastic/foobar
policy_templates:
  - name: system
    title: System logs and metrics
    description: Collect logs and metrics from System instances
    inputs:
      - type: winlog
        title: 'Collect events from the Windows event log'
        description: 'Collecting events from Windows event log'
        condition: ${host.platform} == 'windows'

