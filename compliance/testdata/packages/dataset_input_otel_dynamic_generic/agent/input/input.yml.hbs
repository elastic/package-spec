receivers:
  otlp:
    protocols:
      grpc:
        endpoint: {{endpoint}}
      http: {}

service:
  pipelines:
    logs:
      receivers: [otlp]
    metrics:
      receivers: [otlp]
    traces:
      receivers: [otlp]
